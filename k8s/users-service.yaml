apiVersion: v1
kind: Service
metadata:
  name: snmsd-users-service
spec:
  selector: # select the pod
    app: snmsd-users
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 5001
      targetPort: 5001